const fetchWithTimeout = require('../utils/requests').fetchWithTimeout;
const falsy_to_empty = require('../utils/requests').falsy_to_empty;

async function create_review(review, ip, fetch_timeout) {
};

async function get_review(id, ip, fetch_timeout) {
};

async function delete_review(id, ip, fetch_timeout) {
};

async function update_review(review, ip, fetch_timeout) {
};

module.exports = {create_review, get_review, delete_review, update_review};